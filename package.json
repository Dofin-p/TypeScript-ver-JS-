{
  "name": "ts-version-js-beginner-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:gh": "tsc && vite build --mode gh-pages",
    "preview:gh": "vite preview --mode gh-pages"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  }
}
